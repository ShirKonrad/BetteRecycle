import React, { useEffect, useRef, useState } from "react";
import "./Home.css";

interface Props {}

const Home: React.FC<Props> = (props: Props) => {
  return (
    <span>hello world
    </span>
  );
};

export default Home;
